# Generative Config

Use this file when AI coders are expected to generate large portions of UI, flows, or module skeletons.

## Rules

- Generate from the shared folder tree first
- Respect `cs` or `sw` topology before writing code
- Avoid inventing ad hoc service contracts when a module contract can be explicit
- Keep naming stable so future agents can continue the project safely

