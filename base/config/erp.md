# ERP Config

Use this file when AI coders build broad ERP systems with many modules.

## Rules

- Name modules after business functions, not temporary implementation details
- Keep module structure consistent across departments
- Define each module's execution owner in `sw` mode
- Keep `task`, `pos`, `pay`, and other critical modules independently movable

