# UI Config

Use this file when AI coders need consistent visual output.

## Rules

- Reuse typography, spacing, and layout patterns across modules
- Prefer a coherent design system over one-off screens
- Keep enterprise interfaces dense but readable
- Make components reusable across `website`, `app`, and `erp`

